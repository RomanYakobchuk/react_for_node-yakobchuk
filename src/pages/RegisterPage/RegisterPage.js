import {Link} from "react-router-dom";
import {RegisterForm} from "../../components";

export const RegisterPage = () => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

